{
  "name": "hwplensweb",
  "version": "1.0.0",
  "private": true,
  "description": "Static scroll-led landing page for HwpLens",
  "scripts": {
    "start": "node server.cjs",
    "test": "node scripts/validate.mjs && node --test tests/smoke.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
